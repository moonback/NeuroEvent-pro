# TODO
- [ ] Fix Vite build error in `src/pages/MissionBriefs.tsx` caused by invalid `<img \n ...>` markup (remove stray backslashes/newline escapes).
- [ ] Re-run `npm run build` to confirm the build passes.

